# URL of the FluentPath backend API (no trailing slash).
# The web-admin SPA calls this from the browser, so it must be reachable by the
# clinician's machine (CORS is open in the backend for local/dev).
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
