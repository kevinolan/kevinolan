# Stutter-detection training / fine-tuning pipeline
# Core (already being installed): torch, torchaudio, librosa
# librosa pulls in soundfile (used for audio I/O), numpy is a torch dependency.
torch>=2.0
torchaudio>=2.0
librosa>=0.10
numpy>=1.23
